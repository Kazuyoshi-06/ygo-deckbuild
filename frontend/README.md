# YGO Intel — Frontend

SvelteKit 2 + Svelte 5 app. Proxies `/api` and `/media` to the backend in dev.

## Dev

```bash
npm install
npm run dev      # http://localhost:5173
```

## Build

```bash
npm run build
node build       # production Node server on PORT (default 3000)
```
